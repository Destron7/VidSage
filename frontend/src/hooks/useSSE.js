// useSSE — Server-Sent Events consumer
// Listens to backend SSE endpoint, dispatches events to Zustand stores
// Events: term_detected, transcript_chunk, note_added, voice_state, job_status, brief_ready
